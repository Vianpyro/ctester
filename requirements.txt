fastapi==0.141.1
pydantic==2.13.5
starlette==1.6.0
# Not uvicorn[standard]: wsproto alone provides WebSockets. Without it every
# handshake gets a silent 501.
uvicorn==0.53.0
h11==0.16.0
wsproto==1.3.2
psycopg[binary]==3.3.5
