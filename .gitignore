__pycache__/

# Catalogue public regenere par le worker au demarrage (publish_catalogue()).
# Versionne, il rendrait sale l arbre de /opt/ctester/src a chaque demarrage et
# ferait echouer le `git pull --ff-only` du timer de deploiement.
app/tps.json
app/quiz/
app/tp/

# Le harnais de la page a une dependance de TEST (jsdom, voir package.json) :
# elle donne un vrai DOM a DOMPurify pour que les tests XSS soient reels.
# L application, elle, n a aucune dependance npm.
node_modules/
